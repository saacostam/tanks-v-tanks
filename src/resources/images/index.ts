import { ImageSource } from "excalibur";

import { BASE_URL } from "../../constants";

export const brickImage = new ImageSource(`${BASE_URL}brick.png`);
export const floorImage = new ImageSource(`${BASE_URL}floor.png`);
export const tankImage = new ImageSource(`${BASE_URL}tank.png`);
